/** Static files in `public/` are served from the site root. */
export const ASSETS = {
  /** Browser tab icon */
  favicon: '/brand/logo.png',
  /** Main logo mark (light surfaces) */
  logo: '/brand/logo.png',
  /** Main logo mark (dark surfaces) */
  logoDark: '/brand/logo-dark.png',
  /** Open Graph / Twitter share image (replace with a dedicated 1200×630 asset when ready) */
  ogImage: '/brand/logo.png',
} as const
