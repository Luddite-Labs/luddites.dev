# Canonical site origin (no trailing slash). Used for OG tags, canonical URLs, sitemap, robots.
VITE_SITE_URL=https://luddites.dev

# Contact form → email via Web3Forms (https://web3forms.com). No backend required.
# Restrict the key to your domain in the Web3Forms dashboard.
VITE_WEB3FORMS_ACCESS_KEY=
